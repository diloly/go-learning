server:
  port: 8080
  mode: debug       # debug / release / test

database:
  driver: mysql           # mysql / postgres
  host: 127.0.0.1
  port: 3306
  user: root
  password: "123456"
  dbname: mini_blog
  max_open_conns: 100
  max_idle_conns: 10

jwt:
  secret: "mini-blog-jwt-secret-key-change-in-production"
  expire: 24        # token 有效期（小时）
