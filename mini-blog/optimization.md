是否每次都要迁移表？

需要超级管理员身份
article可以被超级管理员删除（新增超级管理员角色。）

偏移offset分页和游标分页 偏移在数据量少时或需要总页数进行跳页时用。游标分页更像是抖音，小红书这样的流。

为什么token不是在service中生成而是在handler中生成的，是在handler生成比较好吗？

jwt加密，用的对称加密，对多服务器微服务不友好，需要共享公钥，建议使用非对称，比如RSA

登录退出后，怎么清理token

分类 也需要创建和修改功能

四层负载均衡（硬件 / 云 LB）、七层网关、服务注册中心本地负载均衡，各司其职。
